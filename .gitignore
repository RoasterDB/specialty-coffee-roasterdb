# This is the PUBLIC showcase repo — never commit the full dataset or any
# pipeline source here. Only the sample, docs, and assets belong.

# Full dataset / exports (paid deliverable — keep OUT of this repo)
roasterdb_export.*
roasterdb.sqlite
*.sqlite
*.sqlite3
roasterdb_v*.*
# ...but DO keep the public sample
!samples/roasterdb_sample.csv

# Never bring the engine source into this repo
src/
crawler*.py
extractor*.py
normalizer*.py
pilot*.py

# OS / tooling
__pycache__/
*.py[cod]
.DS_Store
Thumbs.db
